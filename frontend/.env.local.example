# Pitwall IQ frontend — copy to frontend/.env.local
# Base URL of the backend API (local default shown). Point at your deployed
# backend in production. Never contains secrets.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
